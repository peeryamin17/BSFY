# KATHE 2026 - English to Kashmiri translation
# Install with: pip install -r requirements.txt
#
# NOTE: Install PyTorch separately to match your CUDA version if you are
# not on a CPU-only machine. See https://pytorch.org/get-started/locally/

torch>=2.0.0
transformers>=4.30.0
datasets>=2.10.0
sacrebleu>=2.3.1
nltk>=3.8.1
pandas>=1.5.3
numpy>=1.24.3
scikit-learn>=1.2.0
huggingface-hub>=0.16.1
accelerate>=0.20.0
sentencepiece>=0.1.99
tensorboard>=2.12.0
pyyaml>=6.0

# Optional: LoRA adapters for memory-efficient fine-tuning
# peft>=0.4.0
